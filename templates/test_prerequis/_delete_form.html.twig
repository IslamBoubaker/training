<form method="post" action="{{ path('app_test_prerequis_delete', {'id': test_prerequi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_prerequi.id) }}">
    <button class="btn">Delete</button>
</form>
